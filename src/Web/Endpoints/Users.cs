using CPHDevFest9neo.Infrastructure.Identity;

namespace CPHDevFest9neo.Web.Endpoints;

public class Users : EndpointGroupBase
{
    public override void Map(WebApplication app)
    {
        app.MapGroup(this)
            .MapIdentityApi<ApplicationUser>();
    }
}
